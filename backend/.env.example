# Copie este arquivo para ".env" e ajuste os valores conforme necessário.

# Chave usada para assinar os tokens JWT de login. NUNCA reaproveite este
# valor de exemplo nem deixe uma chave fixa no código-fonte.
# Gere uma própria com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=troque-esta-chave

# Chave usada para autorizar o disparo do pipeline de testes (rota /api/run-tests).
# Gere uma chave própria, por exemplo com: python -c "import secrets; print(secrets.token_hex(16))"
TEST_RUNNER_API_KEY=troque-esta-chave-tambem

# Origens (frontend) autorizadas a chamar a API, separadas por vírgula.
ALLOWED_ORIGINS=http://localhost:5173

# Opcional: envio de e-mail de confirmação ao criar um agendamento com
# e-mail do cliente preenchido (backend/notifications.py). Sem essas
# variáveis, o app funciona normal — só não manda o e-mail (fica só no
# log do servidor). Exemplo usando Gmail: SMTP_HOST=smtp.gmail.com,
# SMTP_PORT=587, SMTP_USERNAME=seu-email@gmail.com, e SMTP_PASSWORD
# sendo uma "senha de app" (myaccount.google.com/apppasswords) — a
# senha normal da conta NÃO funciona aqui.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=

# Opcional: lembrete automático "faltam 2h para o seu horário" via Telegram
# (backend/notifications.py). É grátis: crie um bot conversando com o
# @BotFather no Telegram (comando /newbot), copie o token que ele te der
# aqui, e o @username do bot (sem o @) em TELEGRAM_BOT_USERNAME — o
# frontend usa isso para montar o link de ativação (t.me/<username>).
# Sem TELEGRAM_BOT_TOKEN, o app funciona normal e só não manda o lembrete.
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=
